%p Edit this profile:
= render 'profiles/form', profile: profile
/%p= link_to 'Delete profile', user_profile_path(@user,profile), method: :delete, class: 'Form-submit bg-red-300 hover:text-red-500 hover:bg-red-300'
