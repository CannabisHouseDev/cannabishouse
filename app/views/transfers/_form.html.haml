= form_for @transfer do |f|
  - if @transfer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@transfer.errors.count, "error")} prohibited this transfer from being saved:"
      %ul
        - @transfer.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :sender_material
    = f.text_field :sender_material
  .field
    = f.label :receiver_material
    = f.text_field :receiver_material
  .field
    = f.label :sender
    = f.text_field :sender
  .field
    = f.label :receiver
    = f.text_field :receiver
  .field
    = f.label :weight
    = f.number_field :weight
  .actions
    = f.submit t('views.transfers.save')
    
