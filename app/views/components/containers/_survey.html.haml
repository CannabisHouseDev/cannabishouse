.w-full.h-full.flex.flex-col
  .text-2xl.capitalize #{local_assigns[:s].title}
  .text-md.grey--text #{local_assigns[:s].description}
  - local_assigns[:s].questions.each do |q|
    = render 'components/containers/question_row', q: q
